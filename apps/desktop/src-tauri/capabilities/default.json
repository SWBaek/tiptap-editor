{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum native dialog permissions for the SDoc editor main window.",
  "windows": ["main"],
  "permissions": ["dialog:allow-open", "dialog:allow-save"]
}
