{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop window permissions for the SDoc editor shell.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
